version: "2.0"
mode: workflow_graph
workflow:
  settings:
    entry_task: start
    max_time_seconds: 30
    parallel_limit: 1
    continue_on_error: false
    max_task_iterations: 3
    max_workflow_iterations: 10
    io:
      result_map:
        status: ok
      output_schema:
        type: object
        properties:
          status:
            type: string
            enum:
              - ok
              - failed
        required:
          - status
  tasks:
    - id: start
      operator: NoOpOperator
      params: {}
      terminal: success
