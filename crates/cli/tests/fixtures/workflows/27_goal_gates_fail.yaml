version: "2.0"
mode: "workflow_graph"
metadata:
  name: "Scenario 27: Goal Gates Failure (WFG-GATE-001)"
workflow:
  settings:
    entry_task: "start"
    completion:
      require_goal_gates: true
      goal_gate_failure_behavior: fail
  tasks:
    - id: "start"
      operator: "NoOpOperator"
      terminal: success
    - id: "required_gate"
      operator: "NoOpOperator"
      goal_gate: true
      terminal: success
