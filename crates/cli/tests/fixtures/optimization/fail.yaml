version: '2.0'
mode: workflow_graph
workflow:
  settings:
    entry_task: failed
    io:
      result_map: {decision: none}
  tasks:
    - id: failed
      operator: NoOpOperator
      terminal: failure
