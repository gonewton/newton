version: '2.0'
mode: workflow_graph
workflow:
  settings:
    entry_task: wait
    io:
      result_map:
        candidate: null
        evaluation: null
  tasks:
    - id: wait
      operator: CommandOperator
      params:
        cmd: sleep 30
      terminal: success
