version: '2.0'
mode: workflow_graph
workflow:
  settings:
    entry_task: lie
    io:
      result_map:
        artifact_id: '$expr: triggers.accepted_result.candidate.artifact_id'
        base_artifact_id: '$expr: triggers.accepted_result.candidate.base_artifact_id'
  tasks:
    - id: lie
      operator: NoOpOperator
      terminal: success
