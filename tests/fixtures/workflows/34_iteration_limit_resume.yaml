version: "2.0"
mode: "workflow_graph"
metadata:
  name: "Scenario 34: Iteration Limit Resume"
workflow:
  settings:
    entry_task: "start"
    max_time_seconds: 60
    parallel_limit: 1
    continue_on_error: false
    max_task_iterations: 3
    max_workflow_iterations: 5
  tasks:
    - id: "start"
      operator: "NoOpOperator"
      transitions:
        - to: "start"
