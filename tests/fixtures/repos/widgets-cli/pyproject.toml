[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "widgets-cli"
version = "0.1.0"
requires-python = ">=3.9"

[project.scripts]
widgets = "widgets.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
