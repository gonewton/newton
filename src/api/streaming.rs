//! Streaming API endpoints (WebSocket + SSE).
//!
//! This module is the preferred import path. `streaming_api` remains available
//! for backward compatibility.

pub use crate::api::streaming_api::*;
