/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * Task definition consumed by the workflow executor.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "WorkflowTask".
 */
export type WorkflowTask = {
  [k: string]: unknown;
} & {
  classes?: string[];
  goal_gate?: boolean;
  goal_gate_group?: string | null;
  id: string;
  include_if?: Condition | null;
  max_iterations?: number | null;
  name?: string | null;
  operator:
    | "AgentOperator"
    | "AssertCompletedOperator"
    | "ChangeRequestOperator"
    | "CommandOperator"
    | "GhOperator"
    | "GitOperator"
    | "GraderAgentOperator"
    | "GraderCommandOperator"
    | "HumanApprovalOperator"
    | "HumanDecisionOperator"
    | "NoOpOperator"
    | "ReadControlFileOperator"
    | "ReconcileOperator"
    | "SetContextOperator"
    | "WorkflowOperator"
    | "barrier";
  parallel_group?: string | null;
  params?: {
    [k: string]: unknown;
  };
  retry?: RetryPolicy | null;
  terminal?: TerminalKind | null;
  timeout_ms?: number | null;
  transitions?: Transition[];
};
/**
 * Condition used to guard transitions between tasks.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "Condition".
 */
export type Condition =
  | {
      $expr: string;
    }
  | boolean;
/**
 * Terminal task kind — determines how the workflow outcome is affected by a terminal task.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "TerminalKind".
 */
export type TerminalKind = "success" | "failure";
/**
 * Allowed trigger types for workflow graphs.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "TriggerType".
 */
export type TriggerType = "manual" | "webhook";
/**
 * Artifact cleanup policy.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "ArtifactCleanupPolicy".
 */
export type ArtifactCleanupPolicy = "lru";
/**
 * Workflow task entries can be concrete tasks or macro invocations pre-transform.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "TaskOrMacro".
 */
export type TaskOrMacro = WorkflowTask | MacroInvocation;
/**
 * Context fidelity policy for agent operators (schema-only; not yet implemented).
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "ContextFidelity".
 */
export type ContextFidelity = "full" | "summary" | "truncate";
/**
 * Controls whether a reached-but-failed goal gate causes the workflow to fail.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "GoalGateFailureBehavior".
 */
export type GoalGateFailureBehavior = "fail" | "allow";

/**
 * Root document for a workflow graph definition.
 */
export interface WorkflowDocument {
  macros?: MacroDefinition[] | null;
  metadata?: WorkflowMetadata | null;
  mode: string;
  triggers?: WorkflowTrigger | null;
  version: string;
  workflow: WorkflowDefinition;
}
/**
 * Reusable macro definition containing one or more task templates.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "MacroDefinition".
 */
export interface MacroDefinition {
  name: string;
  tasks: WorkflowTask[];
}
/**
 * Retry configuration for a task.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "RetryPolicy".
 */
export interface RetryPolicy {
  backoff_ms: number;
  backoff_multiplier?: number | null;
  jitter_ms?: number | null;
  max_attempts: number;
}
/**
 * Transition between tasks.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "Transition".
 */
export interface Transition {
  include_if?: Condition | null;
  label?: string | null;
  priority?: number;
  to: string;
  when?: Condition | null;
}
/**
 * Metadata embedded with a workflow document.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "WorkflowMetadata".
 */
export interface WorkflowMetadata {
  description?: string | null;
  name?: string | null;
  tags?: string[] | null;
}
/**
 * Workflow trigger definition supporting manual and webhook workflows.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "WorkflowTrigger".
 */
export interface WorkflowTrigger {
  payload?: {
    [k: string]: unknown;
  };
  schema_version: string;
  type: TriggerType;
}
/**
 * Workflow-level configuration.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "WorkflowDefinition".
 */
export interface WorkflowDefinition {
  context?: {
    [k: string]: unknown;
  };
  settings: WorkflowSettings;
  tasks: TaskOrMacro[];
}
/**
 * Execution settings for a workflow graph.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "WorkflowSettings".
 */
export interface WorkflowSettings {
  /**
   * Opt-in flag for the Rhai `env()` expression function (spec 074 S8).
   * `env()` reads `std::env::var` at expression-eval time, which makes
   * param resolution environment-dependent and non-deterministic; it is
   * therefore disabled unless a workflow explicitly sets this to `true`.
   */
  allow_env_fn?: boolean;
  artifact_storage?: ArtifactStorageSettings;
  checkpoint?: CheckpointSettings;
  command_operator?: CommandOperatorSettings;
  completion?: CompletionSettings;
  continue_on_error?: boolean;
  /**
   * Default coding engine for all agent operators in this workflow.
   */
  default_engine?: string | null;
  entry_task?: string;
  human?: HumanSettings;
  io?: IoBlock;
  io_settings?: IoSettings;
  max_task_iterations?: number;
  max_time_seconds?: number;
  max_workflow_iterations?: number;
  /**
   * Default model configuration for agent operators.
   * Allowed and silently ignored when the workflow has no AgentOperator tasks.
   */
  model_stylesheet?: ModelStylesheet | null;
  parallel_limit?: number;
  redaction?: RedactionSettings;
  required_triggers?: string[];
  /**
   * Whether to stream agent engine stdout to the terminal by default.
   * Individual agent tasks can override this setting.
   */
  stream_agent_stdout?: boolean;
  webhook?: WebhookSettings;
}
/**
 * Artifact storage configuration embedded in workflow settings.
 */
export interface ArtifactStorageSettings {
  base_path: string;
  cleanup_policy: ArtifactCleanupPolicy;
  max_artifact_bytes: number;
  max_inline_bytes: number;
  max_total_bytes: number;
  retention_hours: number;
}
/**
 * Checkpointing configuration embedded in workflow settings.
 */
export interface CheckpointSettings {
  checkpoint_enabled: boolean;
  checkpoint_interval_seconds: number;
  checkpoint_keep_history: boolean;
  checkpoint_on_task_complete: boolean;
}
/**
 * Command operator specific settings embedded in workflow settings.
 */
export interface CommandOperatorSettings {
  allow_shell: boolean;
}
/**
 * Completion policy configuration for workflow graphs.
 */
export interface CompletionSettings {
  /**
   * Controls whether a reached-but-failed goal gate causes the workflow to fail.
   */
  goal_gate_failure_behavior?: "fail" | "allow";
  require_goal_gates?: boolean;
  stop_on_terminal?: boolean;
  success_requires_no_task_failures?: boolean;
}
/**
 * Human interaction configuration for workflows.
 */
export interface HumanSettings {
  audit_path: string;
  default_timeout_seconds: number;
}
/**
 * Workflow I/O contract: input/output schemas and result mapping.
 */
export interface IoBlock {
  error_schema?: unknown;
  input_schema?: unknown;
  output_schema?: unknown;
  result_map?: {
    [k: string]: unknown;
  } | null;
}
/**
 * Workflow I/O size limits.
 */
export interface IoSettings {
  max_input_bytes?: number | null;
  max_output_bytes?: number | null;
}
/**
 * Workflow-level model configuration for agent operators.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "ModelStylesheet".
 */
export interface ModelStylesheet {
  /**
   * Conversation history retention policy.
   * Schema-only in 017-h — does not affect prompt construction or operator behavior.
   */
  context_fidelity?: "full" | "summary" | "truncate";
  /**
   * Model identifier (e.g. "gpt-4o", "claude-sonnet-4-6").
   */
  model: string;
}
/**
 * Redaction configuration embedded in workflow settings.
 */
export interface RedactionSettings {
  redact_keys?: string[];
}
/**
 * Webhook server configuration embedded in workflow settings.
 */
export interface WebhookSettings {
  auth_token_env: string;
  bind: string;
  enabled: boolean;
  max_body_bytes: number;
}
/**
 * Invocation of a named macro from the workflow task list.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "MacroInvocation".
 */
export interface MacroInvocation {
  macro: string;
  with?: {
    [k: string]: unknown;
  };
}
/**
 * Artifact storage configuration embedded in workflow settings.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "ArtifactStorageSettings".
 */
export interface ArtifactStorageSettings1 {
  base_path: string;
  cleanup_policy: ArtifactCleanupPolicy;
  max_artifact_bytes: number;
  max_inline_bytes: number;
  max_total_bytes: number;
  retention_hours: number;
}
/**
 * Checkpointing configuration embedded in workflow settings.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "CheckpointSettings".
 */
export interface CheckpointSettings1 {
  checkpoint_enabled: boolean;
  checkpoint_interval_seconds: number;
  checkpoint_keep_history: boolean;
  checkpoint_on_task_complete: boolean;
}
/**
 * Command operator specific settings embedded in workflow settings.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "CommandOperatorSettings".
 */
export interface CommandOperatorSettings1 {
  allow_shell: boolean;
}
/**
 * Completion policy configuration for workflow graphs.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "CompletionSettings".
 */
export interface CompletionSettings1 {
  /**
   * Controls whether a reached-but-failed goal gate causes the workflow to fail.
   */
  goal_gate_failure_behavior?: "fail" | "allow";
  require_goal_gates?: boolean;
  stop_on_terminal?: boolean;
  success_requires_no_task_failures?: boolean;
}
/**
 * Human interaction configuration for workflows.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "HumanSettings".
 */
export interface HumanSettings1 {
  audit_path: string;
  default_timeout_seconds: number;
}
/**
 * Workflow-level I/O contract block (optional).
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "IoBlock".
 */
export interface IoBlock1 {
  error_schema?: unknown;
  input_schema?: unknown;
  output_schema?: unknown;
  result_map?: {
    [k: string]: unknown;
  } | null;
}
/**
 * Sub-settings for workflow I/O contract.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "IoSettings".
 */
export interface IoSettings1 {
  max_input_bytes?: number | null;
  max_output_bytes?: number | null;
}
/**
 * Redaction configuration embedded in workflow settings.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "RedactionSettings".
 */
export interface RedactionSettings1 {
  redact_keys?: string[];
}
/**
 * Webhook server configuration embedded in workflow settings.
 *
 * This interface was referenced by `WorkflowDocument`'s JSON-Schema
 * via the `definition` "WebhookSettings".
 */
export interface WebhookSettings1 {
  auth_token_env: string;
  bind: string;
  enabled: boolean;
  max_body_bytes: number;
}
